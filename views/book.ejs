<!DOCTYPE html>
<html>
<head>
    <title>SL | Book</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="/css/sheet.css">
    <!-- <link href='/https://fonts.googleapis.com/css?family=Sofia' rel='stylesheet'> -->

    <!-- Font Awesome -->
    <link rel="stylesheet" href="/https://use.fontawesome.com/releases/v5.6.3/css/all.css">
    <!-- Bootstrap core CSS -->
    <link href="/../css/bootstrap.min.css" rel="stylesheet">
    <!-- Material Design Bootstrap -->
    <link href="/../css/mdb.min.css" rel="stylesheet">
    <!-- Your custom styles (optional) -->
    <link href="/../css/book.css" rel="stylesheet">
</head>

<body>
    <% include partials/header %>
        <center><div class="blockimg">
            <img src="<%= book[0]["owned by"][0].URL %>" class="bimage" >
        </div>
        <h1><%= book[0].title %></h1>
        </center>
        <div class="details">
        <h4>Details</h4>
        <table>
            <tr>
                <th>Field</td>
                <th>Description</td>
            </tr>
            <tr>
                <td>Author</td>
                <td><%= book[0].author.name %></td>
            </tr>
            <tr>
                <td>Publisher</td>
                <td><%= book[0].publisher.name %></td>
            </tr>
            <tr>
                <td>Genre</td>
                <td><%= book[0].genre %></td>
            </tr>
            <tr>
                <td>Language</td>
                <td><%= book[0].language %></td>
            </tr>
            <tr>
                <td>ISBN</td>
                <td><%= book[0].ISBN %></td>
            </tr>
        </table>    
        </div>

        <div class="userlist">
            <span style="font-size:24px; display:block;">Users with the Book</span>
            <div style="display:inline-block;">
                <span style="font-size:20px;">Select Hostel </span>
                <div class="city" id="city">
                        <select class="bselect" name="cities" id="cities" onchange="changecity()">
                        <option id="All India" value="All India" selected>All Hostels</option>
                        </select>
                    </div>
            </div>
            <% for(var i = 0; i< book[0]["owned by"].length; i++) { %>
                <% if( book[0]["owned by"][i].borrowed==false ) { %>
                    <div class="userList" name=<%= book[0]["owned by"][i].user.city%> >
                        <hr>
                        <img style="display: inline-block;" src="<%= book[0]["owned by"][i].URL %>" height=180px width=130px>
                        <div style="display:inline-block; margin:2px; vertical-align: top;">
                            <p>Owner : <%= book[0]["owned by"][i].user.firstname %></p>
                            <p>For <%= book[0]["owned by"][i].status %></p>
                            <p>Price : Rs. <%= book[0]["owned by"][i].price %>/-</p>
                            <a href="/request/<%= book[0]["owned by"][i].id%>"><button class="float-right btn btn-info my-2" type="submit" id="requestButton">Send Request</button></a>
                        </div>
                    </div>
                <% } %>
            <% } %>
                    <hr>
        </div>
        <!-- <div class="post-container" id="profile-details">
                <div class="post-content" id="details">
                    <h1><p class="post-title"><span id="title"><%= book[0].title %></span></p></h1>
                    <div class="tab-pane" id="profile-details">
            <div class="row">
                    <div class="col mb-4">
                        <label>Title</label>
                    </div>
                    <div class="col mb-4">
                        <p><%= book[0].title %></p>
                    </div>
                </div>
            <div class="row">
                <div class="col mb-4">
                    <label>ISBN</label>
                </div>
                <div class="col mb-4">
                    <p><%= book[0].ISBN %></p>
                </div>
            </div>
            <div class="row">
                <div class="col mb-4">
                    <label>Author</label>
                </div>
                <div class="col mb-4">
                    <p><%= book[0].author.name %></p>
                </div>
            </div>
            <div class="row">
                <div class="col mb-4">
                    <label>Publisher</label>
                </div>
                <div class="col mb-4">
                    <p><%= book[0].publisher.name %></p>
                </div>
            </div>
            <div class="row">
                <div class="col mb-4">
                    <label>Genre</label>
                </div>
                <div class="col mb-4">
                    <p><%= book[0].genre %></p>
                </div>
            </div><br>
            <div class="city" id="city">
                    <select class="mdb-select" name="cities" id="cities" onchange="changecity()">
                    <option id="All India" value="All India" selected>All India</option>

                    </select>
                </div>
                <br><hr>
                <div class = "usersOwned" tag = "usersOwned"></div>
            <div id="loopStart">
            <% for(var i = 0; i< book[0]["owned by"].length; i++) { %></div>
            <% if( book[0]["owned by"][i].borrowed==false ) { %>
            <div class="userList" name = <%= book[0]["owned by"][i].user.city%>>
                <div class="row">
                    <a href="/request/<%= book[0]["owned by"][i].id%>"><p><span></span></p>
                    <div id="nameCaption" class="col mb-1">
                        <label>Name</label>
                    </div>
                    <div class="col mb-1">
                        <p id="name"><%= book[0]["owned by"][i].user.firstname %> <%= book[0]["owned by"][i].user.lastname %></p>  <button class="float-right btn btn-info my-2" type="submit" id="requestButton">Send Request</button></a>
                    </div>
                </div>
                <div class="row">
                        <div class="col mb-1">
                            <label>Status</label>
                        </div>
                        <div class="col mb-1">
                            <p>For <%= book[0]["owned by"][i].status %>/-</p>
                        </div>
                    </div>
                <div class="row">
                    <div class="col mb-1">
                        <label>Price</label>
                    </div>
                    <div class="col mb-1">
                        <p><%= book[0]["owned by"][i].price %>/-</p>
                    </div>
                </div>
            </div>
            <% } %>
            <div id="loopEnd">
            <% } %></div>
        </div>
    </div> -->

        <% include partials/footer %>
            <!-- SCRIPTS -->
            <!-- JQuery -->
            <script type="text/javascript" src="/../js/jquery-3.3.1.min.js"></script>
            <!-- Bootstrap tooltips -->
            <script type="text/javascript" src="/../js/popper.min.js"></script>
            <!-- Bootstrap core JavaScript -->
            <script type="text/javascript" src="/../js/bootstrap.min.js"></script>
            <!-- MDB core JavaScript -->
            <script type="text/javascript" src="/../js/mdb.js"></script>
</body>
<script>
    $(document).ready(function() {
        var city = null;

        $.ajax({
            type: 'GET',
            url: 'city', // This one is missing here
            dataType: 'json'
        }).done(function(data){
            city = JSON.stringify(data);
            var len = data.length;
            for( var i = 0; i<len; i++){
                var city = data[i];

                $("#cities").append("<option id='"+city+"' value='"+city+"'>"+city+"</option>");
            }
            <% if(locals.user) { %>
                document.getElementById("All India").selected = false;
                document.getElementById("<%= user.city %>").selected = true;
                changecity();
            <% } %>
        });
    });

    function changecity() {
        if(document.getElementById("cities").value == "All India") {
            userList = document.getElementsByClassName("userList");
            for (i = 0; i < userList.length; i++) {
                userList[i].style.display = "block";
            }
        }
        else {
            userList = document.getElementsByClassName("userList");
            for (i = 0; i < userList.length; i++) {
                userList[i].style.display = "none";
            }
            city = document.getElementById("cities").value;
            listdata = document.getElementsByName(city);
            for (i = 0; i < listdata.length; i++) {
                listdata[i].style.display = "block";
            }
        }
    }

    // window.onload = function changeusers() {
    //     var xmlhttp = new XMLHttpRequest();
    //     var url = 'city';
    //     var isbn = <%= book[0].ISBN %>;
    //     var userId = null;
    //     <% if(locals.user) { %>
    //     userId = <%= user.id %>;
    //     <% } %>
    //     var params = 'ISBN='+isbn+'&id='+userId;
    //     console.log(params);
    //     xmlhttp.open('POST', url, true);
    //     xmlhttp.onreadystatechange = function() {
    //         if (xmlhttp.readyState == 4 && xmlhttp.status == 200) {
    //             changeUsers(xmlhttp);
    //         }
    //     }
    //     xmlhttp.setRequestHeader("Content-Type", "application/x-www-form-urlencoded;charset=UTF-8");
    //     xmlhttp.send(params);

    //     function changeUsers(xmlhttp){
    //         var users = JSON.parse(xmlhttp.responseText);
    //         console.log(users.length);

    //         }
    //     }

    </script>
</html>