<!DOCTYPE html>
<html>
<head>
    <title>Social Library | Profile</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" href="css/sheet.css">
    <link href='https://fonts.googleapis.com/css?family=Sofia' rel='stylesheet'>

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css">
    <!-- Bootstrap core CSS -->
    <link href="css/bootstrap.min.css" rel="stylesheet">
    <!-- Material Design Bootstrap -->
    <link href="css/mdb.min.css" rel="stylesheet">
    <!-- Your custom styles (optional) -->
    <link href="css/market.css" rel="stylesheet">
</head>

<body>
    <% include partials/header %>
    <div class="container">
        <% if (locals.user) { %>
            <div class="col mb-3">
                <div class="profile-head">
                    <h3>Your Profile
                    <a href="/update">
                        <button class="float-right btn btn-info my-2" type="submit">Edit Profile</button>
                    </a>
                </h3>
                    <br> <br>
                </div>
            </div>
            <div class="tab-pane" id="profile-details">
                <div class="row">
                    <div class="col mb-4">
                        <label>Name</label>
                    </div>
                    <div class="col mb-4">
                        <p><%= user.firstname %> <%= user.lastname%></p>
                    </div>
                </div>
                <div class="row">
                    <div class="col mb-4">
                        <label>Email</label>
                    </div>
                    <div class="col mb-4">
                        <p><%= user.email %></p>
                    </div>
                </div>
                <div class="row">
                    <div class="col mb-4">
                        <label>Contact details</label>
                    </div>
                    <div class="col mb-4">
                        <p><%= user.phone %></p>
                    </div>
                </div>
                <div class="row">
                    <div class="col mb-4">
                        <label>City</label>
                    </div>
                    <div class="col mb-4">
                        <p><%= user.city %></p>
                    </div>
                </div>
            </div>
           <div>
                <p>Your books</p>
                <% for(var i = 0; i< book.length; i++) { %>
                    <div class="post-container">
                    <hr>
                    
                    <div class="post-content" id="details">
                      
                        <p><span id="sp">Title : <%= book[i].title %></span></p>
                        <p><span id="mrp">ISBN : <%= book[i].ISBN %></span></p>
                        <p><span id="quantity">Genre <%= book[i].genre %></span></p>
                        <p><span id="expiry">Language : <%= book[i].language %></span></p>
                        <p><span id="expiry">MRP : <%= book[i]["owned by"][0].price %></span></p>

                    </div>
                </div>
                <% } %>
            </div>
        <% } else { %>
            <p>
                You are not a valid user. Please <a href="/register">Sign Up</a>
            </p>
            <p>Have an Account?
                <a href="/signin">Sign In</a>
            </p>
        <% } %>
    </div>
    <% include partials/footer %>
</body>
</html>